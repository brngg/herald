apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: memory-spike
  namespace: default
spec:
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: cartservice
  stressors:
    memory:
      workers: 1
      size: 256MB
